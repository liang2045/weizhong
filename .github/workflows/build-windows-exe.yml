name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build-windows-exe:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build EXE
        run: npm run dist:win

      - name: Upload EXE artifacts
        uses: actions/upload-artifact@v4
        with:
          name: huazai-product-library-windows-exe
          path: dist/*.exe
