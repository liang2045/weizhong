{
  "name": "huazai-product-library",
  "version": "1.0.0",
  "private": true,
  "description": "花再产品库桌面端窗口应用",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check app.js"
  },
  "devDependencies": {
    "electron": "^33.4.11"
  }
}
